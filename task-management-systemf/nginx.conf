server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Proxy API requests to the backend
    location /v1/ {
        proxy_pass https://azentrix-fullstack-task2-sfv8.onrender.com;
        
        # Vital fixes for Render / Cloudflare routing
        proxy_ssl_server_name on;
        proxy_set_header Host azentrix-fullstack-task2-sfv8.onrender.com;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Proxy WebSocket requests to the backend
    location /ws/ {
        proxy_pass https://azentrix-fullstack-task2-sfv8.onrender.com;
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        # Use a dynamic variable to satisfy changing connection status
        proxy_set_header Connection $http_connection; 
        
        # Vital fixes for Render / Cloudflare routing
        proxy_ssl_server_name on;
        proxy_set_header Host azentrix-fullstack-task2-sfv8.onrender.com;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
